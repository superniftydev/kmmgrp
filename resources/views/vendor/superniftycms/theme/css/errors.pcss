body#error-parent-page {

    h1 {
        font-size: 1em;
        color: var(--color-gray-600);
    }

}


main#site-offline {
    position: fixed;
    inset: 0;
    height: 100vh;
    background-color: #fff;

    h1 {
        font-size: 10vw;
        margin: 0 auto;
    }
    p {
        font-size: .875rem;
    }
}
